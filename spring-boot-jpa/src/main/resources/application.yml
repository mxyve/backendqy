spring:
  datasource:
    url: jdbc:mysql://localhost:6033/spring_boot?useUnicode=true&characterEncoding=utf-8&useSSL=false
    username: root
    password: 123456
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    properties:
      hibernate:
        hbm2ddl:
          auto:update # 自动创建、更新、验证数据库表结构、
    show-sql: true